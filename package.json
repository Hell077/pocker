{
  "name": "pocker-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^9.25.1",
    "eslint-plugin-react": "^7.37.5",
    "husky": "^8.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx}": [
      "pnpm -r --filter frontendv2 exec eslint --fix"
    ]
  }
}
